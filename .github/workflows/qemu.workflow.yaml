name: qemu-test
on:
    push:
        tags-ignore:
            - '*'
        branches:
            - '*'
jobs:
    start-stop-test:
#        container: cuaesd/aesd-autotest:24-assignment7-buildroot
        runs-on: self-hosted
        timeout-minutes: 120
        steps:
          - uses: actions/checkout@v2
          - name: Build image
            run: ./tests/validate.sh build
          - name: Start image
            run: ./tests/validate.sh start
          - name: Stop image
            run: ./tests/validate.sh stop
          - name: Cleanup
            if: always()
            run: |
              ssh-add -D
